Welcomd to Manish DevOps World
In this Folder we are creating a ArgoCD Project

First Create Instance
now need to install Docker and kind-cluster, so run install.sh file

    ./install.sh
In Second step need to config kind-cluster created on config.yml
Create a 3-node Kubernetes cluster using Kind:

    kind create cluster --config=config.yml --name=my-cluster
    
